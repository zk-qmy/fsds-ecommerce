[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["a_data_generator", "b_schema_pipelines", "config", "c_drift_labels", "d_ml"]

[project]
name = "fsds-ecommerce"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.43.40",
    "delta-spark>=4.1.0",
    "deltalake>=1.6.1; sys_platform != 'win32'",
    "faker>=40.21.0",
    "fastapi>=0.136.3",
    "minio>=7.2.20",
    "mlflow>=3.13.0",
    "numpy>=2.0.0",
    "pandas>=2.3.3",
    "pyarrow>=24.0.0",
    "pydantic-settings>=2.14.1",
    "pyspark==4.1.0",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.2",
    "ruff>=0.15.15",
    "scikit-learn>=1.9.0",
    "uvicorn>=0.48.0",
]
