connector.name=postgresql
# fsds-postgres — the same instance build_gold.py/feat_*.py write Gold + feature
# tables into via JDBC (b_schema_pipelines/pipelines/gold/build_gold.py's
# _write_postgres). Lets Trino join those tables against the Bronze/Silver
# Delta tables the `delta` catalog above already reaches, in one query.
connection-url=jdbc:postgresql://postgres:5432/fsds
connection-user=fsds
connection-password=fsds
